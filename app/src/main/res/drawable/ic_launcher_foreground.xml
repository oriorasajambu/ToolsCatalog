<?xml version="1.0" encoding="utf-8"?>
<!--
  Toolbox launcher — foreground layer (production).
  Just the shared glyph. The development flavor overrides this file (src/development) to stack a
  "DEV" badge on top; keeping this a layer-list means both flavors share one glyph source.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@drawable/ic_launcher_glyph" />
</layer-list>
