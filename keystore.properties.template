# Release signing credentials for the `toolbox` keystore at the repository root.
#
# Copy this file to `keystore.properties` (gitignored) and fill in the values. The build signs the
# release variants with them; without this file the release build is left unsigned. The keystore
# file itself (`toolbox`) is also gitignored — keep both out of version control.
storePassword=
keyAlias=
keyPassword=
