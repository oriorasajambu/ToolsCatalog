<?xml version="1.0" encoding="utf-8"?>
<!--
  Spacing tokens for this feature. Values live here rather than as `16.dp` literals in the
  composables so a density change is one edit, matching every other feature in the repo.
-->
<resources>
    <dimen name="exifstrip_spacing">16dp</dimen>
    <dimen name="exifstrip_spacing_tight">8dp</dimen>
</resources>
