<?xml version="1.0" encoding="utf-8"?>
<!--
  Spacing and drawing tokens for this feature. Values live here rather than as `16.dp` literals in
  the composables so a density change is one edit, matching every other feature in the repo.
-->
<resources>
    <dimen name="speedometer_spacing">16dp</dimen>
    <dimen name="speedometer_spacing_tight">8dp</dimen>

    <!-- The satellite signal strips, tall enough to compare at a glance. -->
    <dimen name="speedometer_bars_height">48dp</dimen>
</resources>
