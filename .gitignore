*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
/.claude
local.properties

# Per-machine environment config. The committed *.properties.template files hold the defaults;
# copy them to these (gitignored) files to override BASE_URL per checkout.
/dev.properties
/prod.properties

# Release signing — the keystore and its credentials never go in VCS.
# keystore.properties.template is committed; copy it to keystore.properties and fill it in.
/toolbox
/keystore.properties

# Stray release build output (real outputs live under app/build/).
/app/production/
